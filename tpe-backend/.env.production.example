# Production Environment Configuration
# Copy this file to .env.production on the production server
# ⚠️ NEVER commit the actual .env.production file with real passwords

NODE_ENV=production
PORT=5000

# PRODUCTION Database Configuration (AWS RDS)
DB_HOST=tpe-database-production.cmtcsi0kytrf.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=tpedb
DB_USER=tpeadmin
DB_PASSWORD=dBP0wer100!!
DB_SSL=true

# JWT Configuration
JWT_SECRET=your_production_jwt_secret_key_power100_2025

# Frontend URL
FRONTEND_URL=https://tpx.power100.io

# Database URL with SSL (alternative connection method)
DATABASE_URL=postgresql://tpeadmin:dBP0wer100!!@tpe-database-production.cmtcsi0kytrf.us-east-1.rds.amazonaws.com:5432/tpedb?sslmode=require

# Optional: Twilio Configuration (for SMS)
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_PHONE_NUMBER=+1234567890

# ⚠️ IMPORTANT: Production password is: dBP0wer100!!
# This is DIFFERENT from the local development password
# TPE Production Environment Variables Template
# Copy this file to .env.production and fill in the actual values
# NEVER commit .env.production with real credentials to git

# ==============================================================================
# IMPORTANT: Environment File Hierarchy
# ==============================================================================
# Dotenv loads files in this order (later files override earlier ones):
# 1. .env.production (this file)
# 2. .env.local (if exists - OVERRIDES everything)
#
# FOR LOCAL DEVELOPMENT: .env.local takes precedence over .env.production
# FOR DOCKER/PRODUCTION: Only .env.production is used (no .env.local in container)

# ==============================================================================
# CRITICAL: Production Security
# ==============================================================================
# These MUST be different from development values!
DB_PASSWORD=your-secure-database-password-here
JWT_SECRET=your-64-character-production-jwt-secret-here-change-this
TPX_N8N_API_KEY=your-production-n8n-api-key

# ==============================================================================
# Database Configuration (AWS RDS)
# ==============================================================================
DB_HOST=tpe-database-production.cmtcsi0kytrf.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=tpedb
DB_USER=tpeadmin
# DB_PASSWORD - set above

# ==============================================================================
# AWS Configuration
# ==============================================================================
AWS_REGION=us-east-1
AWS_S3_BUCKET=tpe-assets-production-492267598792

# ==============================================================================
# GoHighLevel Integration
# ==============================================================================
GHL_PRIVATE_INTEGRATION_TOKEN=your-ghl-token-here
GHL_LOCATION_ID=your-ghl-location-id-here

# ==============================================================================
# n8n Webhook Configuration
# ==============================================================================
N8N_WEBHOOK_URL=https://n8n.srv918843.hstgr.cloud/webhook/partner-ai-processing

# ==============================================================================
# Communication Services (n8n → GoHighLevel Integration)
# ==============================================================================
# IMPORTANT: TPE does NOT use Twilio or SendGrid directly!
# All email and SMS communications are handled through:
#   TPE Backend → n8n (webhook) → GoHighLevel (GHL)
#
# GHL handles the actual sending of emails and SMS messages
# See GHL_PRIVATE_INTEGRATION_TOKEN and GHL_LOCATION_ID above
#
# NO NEED FOR:
# - SENDGRID_API_KEY (not used)
# - TWILIO_ACCOUNT_SID (not used)
# - TWILIO_AUTH_TOKEN (not used)
#
# All communication configuration is done in GoHighLevel dashboard

# ==============================================================================
# Monitoring & Error Tracking (Optional)
# ==============================================================================
# Uncomment and configure if using Sentry
# SENTRY_DSN=your-sentry-dsn-here
LOG_LEVEL=info

# ==============================================================================
# Feature Flags
# ==============================================================================
ENABLE_SMS_VERIFICATION=true
ENABLE_EMAIL_VERIFICATION=true
ENABLE_AI_COACHING_OPTIN=true
ENABLE_PARTNER_MATCHING=true
ENABLE_CONFIDENCE_SCORING=true
ENABLE_ADMIN_DASHBOARD=true
ENABLE_BULK_OPERATIONS=true
ENABLE_DATA_COLLECTION=true
ENABLE_OUTCOME_TRACKING=true
ENABLE_FOLLOWUP_SCHEDULER=true
USE_S3=true

# ==============================================================================
# Rate Limiting (Production)
# ==============================================================================
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100  # Requests per IP per window
